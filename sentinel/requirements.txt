# --- Core agent frameworks ---
crewai>=0.80.0
crewai-tools>=0.17.0
langgraph>=0.2.0
langchain-core>=0.3.0

# --- LLM providers (pick whichever you have a key for; see .env.example) ---
langchain-groq>=0.2.0
langchain-openai>=0.2.0

# --- MCP server ---
mcp>=1.0.0

# --- NL2SQL / RAG / DB ---
scikit-learn>=1.3.0      # offline TF-IDF RAG backend (no API key needed)
faiss-cpu>=1.8.0          # optional higher-quality RAG backend
numpy>=1.24.0

# --- Utilities ---
python-dotenv>=1.0.0
pydantic>=2.0.0
typing_extensions>=4.9.0
